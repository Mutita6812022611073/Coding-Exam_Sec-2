#include <stdio.h>

int main() {
    int number;
    char score[5],i[5];      

   printf("*** Transaction Fee Calculator***\n ");
    scanf("%s", &score);

    printf("Enter numbre of transactions (3-12): ");
    scanf("%d", &score);
    
   printf("Enter Channel Type for TXN 1 (O,B,A): ");
    scanf("%s", &score);


   printf("Enter Channel Type for TXN 5 (O,B,A): ");
    scanf("%s", &score);

    printf("Enter Total Transaction Value: ");
    scanf("%d", &score);
 

    return 0;
}